// faça dois scripts:
// 1-Sucessor e antecessor, faça uma aplicação que colete um número digitado pelo usuário e logo em seguida mostre em ordem: o número anterior a ele, o próprio número escolhido e o número sucessor a ele

// 2-calcule conta, faça uma aplicação js que pegue o número de clientes em uma mesa, o valor total da conta e após isso divida a conta de forma igual a todos os clientes

const valor = Number(prompt("digite o primeiro numero: "));
const sucessor = valor + 1;
const antecessor = valor - 1;
alert("A soma dos numeros é: " = valor );